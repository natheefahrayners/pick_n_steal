# Pick n Steal – Node.js Database Exercise

This project demonstrates basic CRUD operations using:
- Node.js
- MySQL
- mysql2
- dotenv

## Setup
1. Run `npm install`
2. Create a `.env` file with database credentials
3. Run `node index.js`

No routes were used as per exercise instructions.
